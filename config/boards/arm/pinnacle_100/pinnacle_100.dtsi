/*
 * Copyright (c) 2024 ZMK Firmware
 * SPDX-License-Identifier: Apache-2.0
 */

#include <nordic/nrf52840_qiaa.dtsi>

/ {
	model = "Pinnacle 100";
	compatible = "nordic,pinnacle100", "nordic,nrf52840-qiaa";

	chosen {
		zephyr,console = &uart0;
		zephyr,shell-uart = &uart0;
		zephyr,bt-cfg = &bt;
	};

	leds {
		compatible = "gpio-leds";
		led0: led_0 {
			gpios = <&gpio0 13 GPIO_ACTIVE_LOW>;
			label = "LED 0";
		};
	};

	buttons {
		compatible = "gpio-keys";
		button0: button_0 {
			gpios = <&gpio0 11 GPIO_PULL_UP>;
			label = "Button 0";
		};
	};

	bt {
		compatible = "zephyr,bt-hci";
		zephyr,bt-hci-vendor = "&ipc";
	};

	i2c0: i2c@40003000 {
		compatible = "nordic,nrf-twi";
		reg = <0x40003000 0x1000>;
		interrupts = <1 5>;
		status = "disabled";
		pinctrl-0 = <&i2c0_default>;
		pinctrl-1 = <&i2c0_sleep>;
		pinctrl-names = "default", "sleep";
	};

	spi0: spi@40003000 {
		compatible = "nordic,nrf-spi";
		reg = <0x40003000 0x1000>;
		interrupts = <1 5>;
		status = "disabled";
		pinctrl-0 = <&spi0_default>;
		pinctrl-1 = <&spi0_sleep>;
		pinctrl-names = "default", "sleep";
	};

	spi1: spi@40004000 {
		compatible = "nordic,nrf-spi";
		reg = <0x40004000 0x1000>;
		interrupts = <2 5>;
		status = "disabled";
		pinctrl-0 = <&spi1_default>;
		pinctrl-1 = <&spi1_sleep>;
		pinctrl-names = "default", "sleep";
	};

	uart0: uart0 {
		compatible = "nordic,nrf-uart";
		reg = <0x40002000 0x1000>;
		interrupts = <2 1>;
		status = "disabled";
		pinctrl-0 = <&uart0_default>;
		pinctrl-1 = <&uart0_sleep>;
		pinctrl-names = "default", "sleep";
	};
};

&gpiote {
	status = "okay";
};

&gpio0 {
	status = "okay";
};

&gpio1 {
	status = "okay";
};

&pinctrl {
	uart0_default: uart0_default {
		group1 {
			psels = <NRF_PSEL(UART_TX, 0, 6)>,
				<NRF_PSEL(UART_RX, 0, 5)>;
		};
	};

	uart0_sleep: uart0_sleep {
		group1 {
			psels = <NRF_PSEL(UART_TX, 0, 6)>,
				<NRF_PSEL(UART_RX, 0, 5)>;
			bias-pull-up;
		};
	};

	i2c0_default: i2c0_default {
		group1 {
			psels = <NRF_PSEL(TWI_SCL, 0, 31)>,
				<NRF_PSEL(TWI_SDA, 0, 30)>;
		};
	};

	i2c0_sleep: i2c0_sleep {
		group1 {
			psels = <NRF_PSEL(TWI_SCL, 0, 31)>,
				<NRF_PSEL(TWI_SDA, 0, 30)>;
			bias-pull-up;
		};
	};

	spi0_default: spi0_default {
		group1 {
			psels = <NRF_PSEL(SPIM_SCK, 0, 27)>,
				<NRF_PSEL(SPIM_MOSI, 0, 26)>,
				<NRF_PSEL(SPIM_MISO, 0, 25)>;
		};
	};

	spi0_sleep: spi0_sleep {
		group1 {
			psels = <NRF_PSEL(SPIM_SCK, 0, 27)>,
				<NRF_PSEL(SPIM_MOSI, 0, 26)>,
				<NRF_PSEL(SPIM_MISO, 0, 25)>;
			bias-pull-up;
		};
	};

	spi1_default: spi1_default {
		group1 {
			psels = <NRF_PSEL(SPIM_SCK, 0, 19)>,
				<NRF_PSEL(SPIM_MOSI, 0, 20)>,
				<NRF_PSEL(SPIM_MISO, 0, 22)>;
		};
	};

	spi1_sleep: spi1_sleep {
		group1 {
			psels = <NRF_PSEL(SPIM_SCK, 0, 19)>,
				<NRF_PSEL(SPIM_MOSI, 0, 20)>,
				<NRF_PSEL(SPIM_MISO, 0, 22)>;
			bias-pull-up;
		};
	};
};

&ipc {
	status = "okay";
};
